apiVersion: v1
kind: ConfigMap
metadata:
  name: grepcoin-config
  namespace: grepcoin
data:
  NODE_ENV: "production"
  NEXT_TELEMETRY_DISABLED: "1"
  # Public configuration (non-sensitive)
  NEXT_PUBLIC_APP_URL: "https://grepcoin.io"
  NEXT_PUBLIC_CHAIN_ID: "8453"  # Base Mainnet
  NEXT_PUBLIC_WS_URL: "wss://grepcoin.io"
